#!/usr/bin/env node

'use strict';

process.title = 'netiam';

var program = require( 'commander' ),
    basedir = process.cwd();

console.log( basedir );

program
    .version( '0.0.1' )
    .parse( process.argv );
